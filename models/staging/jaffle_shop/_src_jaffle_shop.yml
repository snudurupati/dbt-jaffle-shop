sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after: 
              count: 100
              period: day
          loaded_at_field: _ETL_LOADED_AT
    tables:
      - name: customers
        freshness: null
      - name: orders

  - name: payments
    database: raw
    schema: stripe
    config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after: 
              count: 100
              period: day
          loaded_at_field: _BATCHED_AT
    tables:
      - name: payment
        
